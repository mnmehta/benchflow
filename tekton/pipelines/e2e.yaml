apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: benchflow-e2e
  labels:
    app.kubernetes.io/name: benchflow
spec:
  description: >
    Generic run-plan pipeline for the BenchFlow end-to-end path. The pipeline
    consumes one immutable RUN_PLAN and delegates platform-specific lifecycle
    steps to the BenchFlow control image.
  params:
    - name: RUN_PLAN
      type: string
      description: Resolved immutable run plan in JSON format.
    - name: BENCHFLOW_IMAGE
      type: string
      default: ghcr.io/albertoperdomo2/benchflow:latest
    - name: SETUP_MODE
      type: string
      default: auto
    - name: TEARDOWN
      type: string
      default: "false"
    - name: MATRIX_RESULTS_CONFIGMAP
      type: string
      default: ""
  workspaces:
    - name: results
    - name: models-storage
    - name: source
    - name: target-kubeconfig
      optional: true
  tasks:
    - name: resolve-run-plan
      taskRef:
        name: resolve-run-plan
      params:
        - name: IMAGE
          value: $(params.BENCHFLOW_IMAGE)
        - name: RUN_PLAN
          value: $(params.RUN_PLAN)

    - name: init-mlflow-run
      runAfter: [resolve-run-plan]
      taskRef:
        name: init-mlflow-run
      params:
        - name: IMAGE
          value: $(params.BENCHFLOW_IMAGE)
        - name: RUN_PLAN
          value: $(params.RUN_PLAN)
        - name: PIPELINE_RUN_NAME
          value: "$(context.pipelineRun.name)"

    - name: setup-run-plan
      runAfter: [init-mlflow-run]
      when:
        - input: "$(tasks.resolve-run-plan.results.stage-deploy)"
          operator: in
          values: ["true"]
      taskRef:
        name: setup-run-plan
      params:
        - name: IMAGE
          value: $(params.BENCHFLOW_IMAGE)
        - name: RUN_PLAN
          value: $(params.RUN_PLAN)
        - name: SETUP_MODE
          value: $(params.SETUP_MODE)
      workspaces:
        - name: state-storage
          workspace: results
          subPath: $(context.pipelineRun.name)/platform-state
        - name: target-kubeconfig
          workspace: target-kubeconfig

    - name: download-model
      runAfter: [setup-run-plan]
      when:
        - input: "$(tasks.resolve-run-plan.results.stage-download)"
          operator: in
          values: ["true"]
      taskRef:
        name: download-model
      params:
        - name: IMAGE
          value: $(params.BENCHFLOW_IMAGE)
        - name: RUN_PLAN
          value: $(params.RUN_PLAN)
      workspaces:
        - name: models-storage
          workspace: models-storage
        - name: target-kubeconfig
          workspace: target-kubeconfig

    - name: deploy
      runAfter: [download-model]
      when:
        - input: "$(tasks.resolve-run-plan.results.stage-deploy)"
          operator: in
          values: ["true"]
      taskRef:
        name: deploy-run-plan
      params:
        - name: IMAGE
          value: $(params.BENCHFLOW_IMAGE)
        - name: RUN_PLAN
          value: $(params.RUN_PLAN)
        - name: PIPELINE_RUN_NAME
          value: "$(context.pipelineRun.name)"
      workspaces:
        - name: source
          workspace: source
        - name: manifests-storage
          workspace: results
          subPath: $(context.pipelineRun.name)/rendered-manifests
        - name: target-kubeconfig
          workspace: target-kubeconfig

    - name: wait-for-endpoint
      runAfter: [deploy]
      when:
        - input: "$(tasks.resolve-run-plan.results.stage-benchmark)"
          operator: in
          values: ["true"]
      taskRef:
        name: wait-for-endpoint
      params:
        - name: IMAGE
          value: $(params.BENCHFLOW_IMAGE)
        - name: RUN_PLAN
          value: $(params.RUN_PLAN)
        - name: ENABLED
          value: $(tasks.resolve-run-plan.results.verify-completions)
      workspaces:
        - name: target-kubeconfig
          workspace: target-kubeconfig

    - name: verify-completions-endpoint
      runAfter: [wait-for-endpoint]
      when:
        - input: "$(tasks.resolve-run-plan.results.stage-benchmark)"
          operator: in
          values: ["true"]
      taskRef:
        name: verify-completions-endpoint
      params:
        - name: IMAGE
          value: $(params.BENCHFLOW_IMAGE)
        - name: RUN_PLAN
          value: $(params.RUN_PLAN)
      workspaces:
        - name: target-kubeconfig
          workspace: target-kubeconfig

    - name: benchmark
      runAfter: [verify-completions-endpoint]
      onError: continue
      when:
        - input: "$(tasks.resolve-run-plan.results.stage-benchmark)"
          operator: in
          values: ["true"]
      taskRef:
        name: run-guidellm-benchmark
      timeout: "2h"
      params:
        - name: IMAGE
          value: $(params.BENCHFLOW_IMAGE)
        - name: RUN_PLAN
          value: $(params.RUN_PLAN)
        - name: PIPELINE_RUN_NAME
          value: "$(context.pipelineRun.name)"
        - name: MLFLOW_RUN_ID
          value: "$(tasks.init-mlflow-run.results.mlflow-run-id)"
      workspaces:
        - name: results
          workspace: results
          subPath: $(context.pipelineRun.name)/benchmark
        - name: target-kubeconfig
          workspace: target-kubeconfig

    - name: collect-artifacts
      runAfter: [benchmark]
      onError: continue
      when:
        - input: "$(tasks.resolve-run-plan.results.stage-collect)"
          operator: in
          values: ["true"]
      taskRef:
        name: collect-artifacts
      params:
        - name: IMAGE
          value: $(params.BENCHFLOW_IMAGE)
        - name: RUN_PLAN
          value: $(params.RUN_PLAN)
        - name: PIPELINE_RUN_NAME
          value: "$(context.pipelineRun.name)"
        - name: MLFLOW_RUN_ID
          value: "$(tasks.init-mlflow-run.results.mlflow-run-id)"
        - name: BENCHMARK_START_TIME
          value: "$(tasks.benchmark.results.benchmark-start-time)"
        - name: BENCHMARK_END_TIME
          value: "$(tasks.benchmark.results.benchmark-end-time)"
      workspaces:
        - name: artifacts
          workspace: results
          subPath: $(context.pipelineRun.name)
        - name: target-kubeconfig
          workspace: target-kubeconfig

    - name: collect-metrics
      runAfter: [collect-artifacts]
      onError: continue
      when:
        - input: "$(tasks.resolve-run-plan.results.stage-collect)"
          operator: in
          values: ["true"]
        - input: "$(tasks.resolve-run-plan.results.stage-benchmark)"
          operator: in
          values: ["true"]
      taskRef:
        name: collect-metrics
      params:
        - name: IMAGE
          value: $(params.BENCHFLOW_IMAGE)
        - name: RUN_PLAN
          value: $(params.RUN_PLAN)
        - name: BENCHMARK_START_TIME
          value: "$(tasks.benchmark.results.benchmark-start-time)"
        - name: BENCHMARK_END_TIME
          value: "$(tasks.benchmark.results.benchmark-end-time)"
        - name: MLFLOW_RUN_ID
          value: "$(tasks.init-mlflow-run.results.mlflow-run-id)"
      workspaces:
        - name: artifacts
          workspace: results
          subPath: $(context.pipelineRun.name)
        - name: target-kubeconfig
          workspace: target-kubeconfig

    - name: upload-to-mlflow
      runAfter: [collect-metrics]
      when:
        - input: "$(tasks.resolve-run-plan.results.stage-collect)"
          operator: in
          values: ["true"]
      taskRef:
        name: upload-to-mlflow
      params:
        - name: IMAGE
          value: $(params.BENCHFLOW_IMAGE)
        - name: RUN_PLAN
          value: $(params.RUN_PLAN)
        - name: MLFLOW_RUN_ID
          value: "$(tasks.init-mlflow-run.results.mlflow-run-id)"
        - name: BENCHMARK_START_TIME
          value: "$(tasks.benchmark.results.benchmark-start-time)"
        - name: BENCHMARK_END_TIME
          value: "$(tasks.benchmark.results.benchmark-end-time)"
        - name: GRAFANA_URL
          value: ""
      workspaces:
        - name: artifacts
          workspace: results
          subPath: $(context.pipelineRun.name)
        - name: target-kubeconfig
          workspace: target-kubeconfig

    - name: publish-matrix-result
      runAfter: [upload-to-mlflow]
      when:
        - input: "$(params.MATRIX_RESULTS_CONFIGMAP)"
          operator: notin
          values: [""]
        - input: "$(tasks.resolve-run-plan.results.stage-benchmark)"
          operator: in
          values: ["true"]
        - input: "$(tasks.resolve-run-plan.results.stage-collect)"
          operator: in
          values: ["true"]
      taskRef:
        name: publish-matrix-result
      params:
        - name: IMAGE
          value: $(params.BENCHFLOW_IMAGE)
        - name: RUN_PLAN
          value: $(params.RUN_PLAN)
        - name: MATRIX_RESULTS_CONFIGMAP
          value: $(params.MATRIX_RESULTS_CONFIGMAP)
        - name: CHILD_EXECUTION_NAME
          value: "$(context.pipelineRun.name)"
        - name: MLFLOW_RUN_ID
          value: "$(tasks.init-mlflow-run.results.mlflow-run-id)"
        - name: BENCHMARK_START_TIME
          value: "$(tasks.benchmark.results.benchmark-start-time)"
        - name: BENCHMARK_END_TIME
          value: "$(tasks.benchmark.results.benchmark-end-time)"

  finally:
    # Tekton forbids runAfter under spec.finally (all finally tasks run in
    # parallel). Keep finalize-before-cleanup by sequencing both in one Task.
    - name: finalize-then-cleanup
      taskRef:
        name: finalize-then-cleanup
      params:
        - name: IMAGE
          value: $(params.BENCHFLOW_IMAGE)
        - name: RUN_PLAN
          value: $(params.RUN_PLAN)
        - name: PIPELINE_RUN_NAME
          value: "$(context.pipelineRun.name)"
        - name: MLFLOW_RUN_ID
          value: "$(tasks.init-mlflow-run.results.mlflow-run-id)"
        - name: RUN_START_TIME
          value: "$(tasks.init-mlflow-run.results.run-start-time)"
        - name: STATUS
          value: FAILED
        - name: REASON
          value: "normal MLflow upload did not complete"
        - name: TEARDOWN
          value: $(params.TEARDOWN)
        - name: STAGE_COLLECT
          value: "$(tasks.resolve-run-plan.results.stage-collect)"
        - name: UPLOAD_STATUS
          value: "$(tasks.upload-to-mlflow.status)"
        - name: STAGE_CLEANUP
          value: "$(tasks.resolve-run-plan.results.stage-cleanup)"
      workspaces:
        - name: artifacts
          workspace: results
          subPath: $(context.pipelineRun.name)
        - name: state-storage
          workspace: results
          subPath: $(context.pipelineRun.name)/platform-state
        - name: target-kubeconfig
          workspace: target-kubeconfig
    - name: assert-benchmark-status
      when:
        - input: "$(tasks.resolve-run-plan.results.stage-benchmark)"
          operator: in
          values: ["true"]
      taskRef:
        name: assert-task-status
      params:
        - name: IMAGE
          value: $(params.BENCHFLOW_IMAGE)
        - name: TASK_NAME
          value: benchmark
        - name: TASK_STATUS
          value: "$(tasks.benchmark.status)"
    - name: assert-metrics-status
      when:
        - input: "$(tasks.resolve-run-plan.results.stage-collect)"
          operator: in
          values: ["true"]
        - input: "$(tasks.resolve-run-plan.results.stage-benchmark)"
          operator: in
          values: ["true"]
      taskRef:
        name: assert-task-status
      params:
        - name: IMAGE
          value: $(params.BENCHFLOW_IMAGE)
        - name: TASK_NAME
          value: collect-metrics
        - name: TASK_STATUS
          value: "$(tasks.collect-metrics.status)"
